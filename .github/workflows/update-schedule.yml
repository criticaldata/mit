name: Update Presentation Schedule

on:
  push:
    branches: [main]
    paths:
      - "data/talks/**"
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"
      - name: Install dependencies
        run: pip install pyyaml
      - name: Regenerate schedule
        run: python scripts/generate_schedule.py > data/talks/README.md
      - name: Commit if changed
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/talks/README.md
          git diff --cached --quiet || git commit -m "chore: regenerate presentation schedule [skip ci]"
          git push
